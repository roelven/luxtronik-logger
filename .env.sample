# Luxtronik Logger Configuration

# Database connection settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=luxtronik
DB_USER=postgres
DB_PASSWORD=yourpassword

# Luxtronik device settings
LUXTRONIK_HOST=192.168.1.100
LUXTRONIK_PORT=80

# CSV generation settings
CSV_OUTPUT_DIR=./data
READABLE_HEADERS=false  # Set to 'true' to use human-readable sensor names in CSV headers
CSV_RETENTION_DAYS=30

# Logging settings
LOG_LEVEL=INFO
